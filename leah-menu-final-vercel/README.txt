这是最终 Vercel 拖拽版。

请上传整个 leah-menu-final-vercel 文件夹。

里面必须包含：
- index.html
- 404.html
- vercel.json
- package.json

上传成功后，请打开 Vercel 新生成的最新地址。
不要继续打开旧的 404 地址。

如果要绑定到旧域名，需要在 Vercel 的 Domains 里把域名指向最新部署。
